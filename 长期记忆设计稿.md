# 长期记忆设计稿

## 1. 目标是什么

为 `ReactAgent` 增加“跨会话可持续使用”的长期记忆能力。

这里的长期记忆不是简单把会话历史持久化，而是要做到：

- 与 `session_id` 解耦，绑定到稳定的 `user_id`
- 后端重启后仍然存在
- 能在新会话中继续被读取和使用
- 只保存高价值、可复用的信息，而不是无差别存所有对话
- 后续可以继续扩展为更完整的记忆体系

## 2. 第一版要实现什么功能

第一版聚焦“结构化长期记忆”，范围控制如下：

- 新增 `user_id` 作为长期记忆绑定主键
- 使用 SQLite 持久化长期记忆
- 维护用户画像 `user_profiles`
- 维护长期记忆条目 `user_memories`
- 在每轮对话开始前读取长期记忆并注入 prompt
- 在每轮回答完成后，基于规则从本轮 query 中提取长期信息并写入
- 保留原有基于 `session_id` 的短期记忆机制，不替代它

第一版暂不实现：

- 向量化长期记忆检索
- 复杂的 LLM 自动抽取
- 用户侧记忆管理页面
- 删除/编辑记忆的管理接口

## 3. 当前设计方案

### 3.1 记忆分层

项目中的记忆被拆成两层：

- 短期记忆
  - 继续由 LangGraph checkpointer 管理
  - 作用域是 `session_id`
  - 负责单次会话中的上下文续接

- 长期记忆
  - 由新的 `MemoryService` 负责
  - 作用域是 `user_id`
  - 负责跨会话保存用户画像、偏好和问题背景

### 3.2 存储结构

新增 SQLite 数据文件：

- `backend/data/memory/long_term_memory.sqlite3`

新增两张表：

- `user_profiles`
  - 保存稳定、结构化的画像字段
  - 如姓名、城市、设备型号、户型、预算、是否有宠物、偏好

- `user_memories`
  - 保存长期记忆条目
  - 如历史问题背景、用户显式要求记住的偏好

### 3.3 运行时接入位置

长期记忆不直接塞进消息历史，而是采用“独立上下文块注入”：

1. 前端请求携带 `query + session_id + user_id`
2. `ChatService` 在调用 `ReactAgent` 之前读取长期记忆
3. `MemoryService` 生成 `memory_context`
4. `ReactAgent` 把 `memory_context` 放进 `runtime.context`
5. `dynamic_prompt` 在系统提示词后追加长期记忆块
6. 回答完成后，`ChatService` 调用 `MemoryService.extract_and_save(...)` 写回

### 3.4 第一版抽取规则

当前规则只提取较稳定的事实：

- `我住在X` -> `location`
- `我叫X` -> `name`
- `我买的是X/型号是X` -> `product_model`
- `预算3000元以内` -> `budget_range`
- `有猫/有狗/有宠物` -> `has_pet`
- `静音/拖地/续航/避障/清洁能力/性价比` -> `preferences`
- `小户型/大户型/两室一厅/三室两厅/复式/别墅` -> `home_type`
- `门槛/地毯/异味/不回充/卡住/噪音` -> `issue_context`
- `记住/以后推荐/下次推荐/偏好` -> `preference`

## 4. 当前已经做到哪一步了

已完成：

- 增加长期记忆配置 `config/memory.yml`
- 增加 SQLite 存储层 `backend/app/infra/memory/sqlite_store.py`
- 增加长期记忆服务 `backend/app/services/memory_service.py`
- `ChatRequest` 新增 `user_id`
- `ChatService` 接入长期记忆读取和写入
- `ReactAgent` 支持接收 `memory_context`
- `middleware` 支持把长期记忆注入动态 prompt
- 前端请求新增 `user_id` 本地持久化
- 各新增核心模块加入 `__main__` 测试代码

未完成但已规划：

- 记忆删除/纠错接口
- 记忆可视化查看
- 向量化语义检索
- 更稳健的规则抽取与冲突合并策略
- LLM 辅助抽取与重要性打分

## 5. 后续计划

建议按以下顺序继续推进：

1. 补一个查看某个 `user_id` 长期记忆的调试接口
2. 增加删除/清空某个用户长期记忆的接口
3. 把 `user_memories` 接入向量库，支持语义检索
4. 优化记忆抽取规则，减少误记和漏记
5. 补全自动化测试

## 6. 第一版验收标准

满足以下行为即可视为第一版完成：

- 同一 `user_id` 下，重置 `session_id` 后仍能读到长期记忆
- 服务重启后长期记忆不丢失
- 用户显式说出的偏好和稳定事实能被后续回答使用
- 不同 `user_id` 之间不会串记忆
