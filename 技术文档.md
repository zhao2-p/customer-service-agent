# 一、项目架构

本项目是一个面向扫地机器人与扫拖一体机器人场景的智能客服系统，整体采用前后端分离架构。

- 前端负责采集用户输入、维护 `session_id` 与 `user_id`、发起流式请求、接收并展示后端返回内容
- 后端负责会话编排、Agent 推理、工具调用、RAG 检索、短期记忆与长期记忆管理
- 知识库负责提供产品知识、维护保养、故障排查等领域资料
- 模型层负责对检索结果、工具结果和会话上下文进行整合，生成最终回答

整体调用链路如下：

1. 前端输入用户问题，并随请求携带 `query + session_id + user_id`
2. FastAPI 接收请求后进入 `ChatService`
3. `ChatService` 先读取长期记忆，构造成 `memory_context`
4. `ReactAgent` 在 `thread_id=session_id` 下运行，短期记忆由 `checkpointer` 托管
5. Agent 根据问题决定是否调用 RAG 检索工具或其他工具
6. 模型结合系统提示词、长期记忆、短期记忆、工具结果生成最终答案
7. `ChatService` 将最终答案以 SSE 流式返回前端
8. 回答完成后，从当前轮用户输入中抽取可复用信息，写入长期记忆

项目中的主要分层如下：

- `frontend/`
  - 负责聊天页交互
  - 不维护完整历史消息，只负责把 `query`、`session_id`、`user_id` 发给后端
- `backend/app/api/`
  - 提供 HTTP 接口
  - 负责接收前端请求并返回标准响应
- `backend/app/services/`
  - 负责业务编排
  - 包括聊天服务、长期记忆服务
- `backend/app/agents/`
  - 负责 Agent 本体、中间件、工具和提示词切换
- `backend/app/infra/`
  - 负责模型工厂、向量库、文件加载、SQLite 存储等基础设施
- `backend/data/`
  - 存放知识库原始文件、外部数据、长期记忆 SQLite 文件
- `config/`
  - 存放 RAG、向量库、Prompt、Agent、Memory 相关配置

# 二、技术栈

- 后端框架：`FastAPI`
- 大模型接入：`LangChain + Tongyi(Qwen)`
- Agent 编排：`LangChain Agent`
- 短期记忆：`LangGraph checkpointer + InMemorySaver`
- 长期记忆：`SQLite`
- 向量数据库：`Chroma`
- 向量模型：`DashScopeEmbeddings`
- 文档加载：`TextLoader`、`PyPDFLoader`
- 文本切分：`RecursiveCharacterTextSplitter`
- 前端：`HTML + CSS + JavaScript`
- 流式传输协议：`SSE (text/event-stream)`

# 三、核心技术与功能

## RAG

### 步骤一：知识库准备

主要是进行文件读取，去重，再载入chroma向量数据库，最后返回一个向量检索器。

构建一个知识库服务对象

1. 在内部声明一个 `Chroma` 向量数据库的实例属性，以及一个 `LangChain` 内置的文本分割器实例属性

2. 在内部声明一个检索器方法，将 Chroma 向量数据库转为向量检索器对象返回

3. 声明一个文件加载方法，在这个方法内部：

- 先获取需要加载的知识文件，然后转成 md5 值，检查 md5 值是否存在，存在则跳过避免重复入库
- 如果文件不存在于向量数据库，就用文本切割器对文件进行切分。切分依据可以根据知识文件的类型选择按照字符数量、token 数量、标点符号、语义等方式来进行切分，同时可以设置块重叠大小
- 切分好之后用 Chroma 的 `add_documents` 方法入库，同时保存 md5 值

4. 主要功能：提供向量检索器和文档加载

当前项目中的具体实现如下：

- 向量库服务类是 `VectorStoreService`
- 持久化目录来自 `config/chroma.yml` 中的 `persist_directory`
- 知识文件目录来自 `config/chroma.yml` 中的 `data_path`
- 已入库文件去重依赖 `backend/chroma_db/md5.text`
- 当前允许的知识文件类型为 `txt` 和 `pdf`
- 当前切块参数为：
  - `chunk_size = 200`
  - `chunk_overlap = 20`
  - `k = 3`
- 文档加载时会先把文件转成统一的 `Document` 结构，再切块后写入 Chroma

### 步骤二：RAG 检索回答

主要是使用识库服务实例的向量检索器来进行向量匹配，对向量匹配的结果进行简单的处理和拼接之后通过一个RAG总结方法返回出去。

构建一个 RAG 检索服务对象

1. 在内部声明一个向量库服务实例，并在初始化时检查向量库是否已经准备完成

- 如果向量库持久化目录和 md5 文件已经存在，则说明知识库已初始化完成
- 如果未初始化，则自动触发文档加载流程，将知识文件导入向量数据库

2. 在内部声明一个检索器属性，用于根据用户问题从向量库中召回相关文档

3. 在内部加载 RAG 专用提示词模板，并创建 “提示词 -> 模型 -> 字符串解析器” 的处理链

4. 当收到用户问题时，先执行检索，再把检索结果整理成统一上下文字符串

- 每个召回文档会被格式化为“参考资料内容 + 元数据”
- 多个文档最终拼接成一个完整的 `context`

5. 把 `query` 和 `context` 一起传给大模型，让模型基于检索结果生成总结回答

6. 主要功能：完成“先检索、后总结”的标准 RAG 回答链路

当前项目中的具体实现如下：

- RAG 服务类是 `RagSummarizeService`
- 入口方法是 `rag_summarize(query)`
- 检索方法是 `retriever.invoke(query)`
- RAG 提示词路径由 `config/prompts.yml` 中的 `rag_summarize_prompt_path` 指定
- 当前 RAG 工具被封装为 Agent 可调用工具 `rag_summarize`

这部分链路的职责边界是：

- 前端只需要提交用户问题，不参与检索逻辑
- 后端负责知识召回、上下文拼接和答案总结
- Agent 负责决定什么时候调用这个工具，而不是所有问题都强制进入 RAG

## 短期记忆

主要是基于 前端传入的`session_id` 让同一会话在后端自动续接上下文，把多轮	对话状态交给 `LangGraph checkpointer` 统一管理。

主要由 `LangGraph` 的 `checkpointer` 和 `InMemorySaver` 实现

1. 在 `ReactAgent` 中，Agent 初始化时注册一个 `checkpointer`。每次请求时通过 config 关键字传入 `thread_id`

2. 对于每一个 `thread_id`，`LangGraph` 的 `checkpointer` 会保存和读取该线程对应的一系列 checkpoints 检查点。每一个 checkpoint 都是某一时刻 graph state 图状态对象的快照

- 目前使用的是 `InMemorySaver`，这些 checkpoint 保存在当前进程内存中，因此通常会随着进程结束而丢失
- 如果使用 `PostgresSaver`，这些 checkpoint 会存入 PostgreSQL 中，因此同一个 `thread_id` 的上下文可以在后续请求中恢复
- 不同 `thread_id` 的状态彼此隔离，不会自动互通。因此即使数据可以在数据库里长期存储，但只要它服务的是某一个 `thread_id` 的会话连续性，它依然属于短期记忆；而跨线程、跨会话共享的用户长期事实，则属于 long-term memory

当前项目中的具体实现如下：

- 前端每次请求只发送本轮 `query`，不再手动拼接完整历史
- 后端把 `session_id` 作为 `thread_id` 传给 Agent
- 只要 `session_id` 不变，同一会话的上下文就会自动续接
- 当前“清空会话”的本质是生成新的 `session_id`
- 后端服务重启后，基于 `InMemorySaver` 的短期记忆会丢失

## 长期记忆

长期记忆不是靠 LangChain 或 LangGraph 自带的记忆机制实现的，我是自己实现的一套基于 user_id + SQLite 的长期记忆方案。具体就是在每次 Agent 调用前，后端根据前端传来的 user_id，从 SQLite 中读取该用户跨会话保存的长期信息，由服务层拼接成长期记忆上下文，再通过动态提示词中间件将长期记忆上下文注入到系统提示词中；在回答结束后，再根据当前用户输入抽取可复用的关键信息并写回数据库。

### 一、长期记忆的存储层

是长期记忆的存储层，主要负责把长期记忆真正保存到 SQLite 中，并提供统一的增删查改基础能力给上层服务调用。

- 初始化数据库和表结构，连接 SQLite 并执行具体 SQL

- 把数据库中的记录转换成 Python 可直接使用的数据结构

维护一个`SQLiteMemoryStore`类，为 `MemoryService` 提供用户画像读取与更新，记忆写入等基础能力。

SQLite中主要维护了两张表

- 结构化信息存放在 `user_profiles` 表中，适合保存姓名、所在地、预算、偏好等相对稳定的用户画像字段
- 非结构化信息存放在 `user_memories` 表中，适合保存历史问题背景、显式偏好等长期记忆条目； 

### 二、长期记忆业务编排

主要负责把用户输入和数据库存储连接起来。

- 在请求进入 Agent 之前读取长期记忆
- 把读取出来的数据整理成模型可消费的 `memory_context`
- 在回答完成后，从当前轮输入中抽取可长期保存的信息
- 调用 `SQLiteMemoryStore` 完成真正的持久化写入

也就是说，`SQLiteMemoryStore` 更偏底层存储，`MemoryService` 更偏业务编排。

`MemoryService` 中三个核心方法的功能与实现

- 构建长期记忆上下文
  - 功能：
    - 根据 `user_id` 读取该用户的长期记忆，并拼装成一段可直接注入 prompt 的上下文
  - 实现方式：
    - 先调用 `self.store.get_user_profile(user_id)` 读取结构化画像
    - 再调用 `self.retrieve_memories(user_id, query)` 读取与当前问题相关的长期记忆条目
    - 把画像字段和记忆条目整理成 `[长期记忆]` 开头的文本块
    - 如果当前没有任何可注入内容，则返回空字符串
- 检索匹配长期记忆
  - 功能：
    - 根据当前用户问题，从该用户已有长期记忆中筛出更相关的记忆条目，返回给 `build_memory_context`
  - 实现方式：
    - 把当前 `query` 按空格和中英文标点切分成若干关键词
    - 根据关键词是否出现在 memory content 中的方式做简单匹配
  - 特点与改进：
    - 这是基于关键词包含关系的轻量检索
    - 后期可以接入向量化的长期记忆语义检索
- 提取并保存长期记忆
  - 功能：
    - 在一轮回答结束后，从当前轮用户输入中提取值得长期保存的信息，并写回数据库
  - 实现方式：
    - 基于用户当前输入文本中的关键词和按模式匹配，抽取结构化画像字段和非结构化长期记忆
  - 改进方法：
    - 考虑更复杂的关键词+模式匹配+ LLM 的抽取

三、改进方法

- 长期记忆检索的时候通过向量检索
- 每次会话基于 LLM 的更复杂抽取与重要性判断
- 定期总结，删除长期记忆内容

## 提示词工程

主要是把 RAG 检索、外部数据读取和场景切换这些能力封装成工具，交给 Agent 按需自主调用。

由提示词配置加载、动态 Prompt 切换和长期记忆注入三部分组成

1. 项目中的提示词文件路径统一放在 `config/prompts.yml`

- `main_prompt_path`：主问答场景系统提示词
- `rag_summarize_prompt_path`：RAG 总结场景提示词
- `report_prompt_path`：报告场景提示词

2. 提示词读取统一由 `prompt_support.py` 负责

- 通过配置读取实际 prompt 文件路径
- 加载为字符串供模型或链路使用
- 出错时统一记录日志

3. 当前存在两类主要提示词使用方式

- Agent 主提示词
  - 用于常规问答、多轮对话、工具调用决策
- RAG 总结提示词
  - 用于“已拿到检索资料后”的总结生成

4. Agent 在真正调用模型前，会通过中间件动态决定本轮使用哪个系统提示词

- 默认使用主提示词
- 如果运行时上下文中 `report=True`，则切换为报告专用提示词

5. 长期记忆注入策略

- 长期记忆不直接作为历史消息写入
- 而是通过 `append_memory_context(base_prompt, memory_context)` 追加到系统提示词后面
- 这样模型可以清楚区分“系统规则”和“用户已知事实”
- 后续如果需要替换成更复杂的 memory prompt 策略，也只需要调整这一层

6. 主要功能：控制模型角色、回答边界、工具使用行为以及长期记忆注入方式

## 工具调用

主要由 Agent 工具列表和工具调用中间件实现

1. 在 `ReactAgent` 初始化时，为 Agent 注册一组可调用工具

当前工具包括：

- `rag_summarize`
  - 从向量库中检索相关资料并生成总结
- `get_weather`
  - 获取城市天气，当前为模拟实现
- `get_user_location`
  - 获取用户所在城市，当前为模拟实现
- `get_user_id`
  - 获取用户 ID，当前为模拟实现
- `get_current_month`
  - 获取当前月份，当前为模拟实现
- `fetch_external_data`
  - 从外部 CSV 数据中读取指定用户指定月份的记录
- `fill_context_for_report`
  - 为报告场景打标，触发提示词切换

2. Agent 会根据用户问题，自主决定是否调用工具，以及调用哪个工具

3. 项目中通过工具调用中间件 `monitor_tool` 统一监控工具执行过程

- 在工具调用前记录工具名和参数
- 在工具执行后记录成功日志
- 在工具失败时记录错误日志
- 如果调用了 `fill_context_for_report`，则在 `runtime.context` 中写入 `report=True`

4. 外部数据工具当前的实现方式

- 使用 `config/agent.yml` 中配置的 `external_data_path`
- 第一次读取 CSV 后缓存到进程内存中
- 后续相同进程内重复调用时直接复用缓存

5. 主要功能：把“知识检索、外部信息读取、场景切换”这些非纯文本能力暴露给 Agent 使用

## 流式输出

主要是把后端最终答案以 SSE 事件流的形式持续返回给前端，让用户以流式方式看到最终回复生成过程。

主要由 `ChatService` 和前端 SSE 消费逻辑共同完成

1. 后端接口使用 `POST /api/v1/chat/stream`

前端请求体只需要提供：

- `query`：当前轮用户输入
- `session_id`：用于标识当前会话，对应短期记忆 thread
- `user_id`：用于标识当前用户，对应长期记忆

前端接收内容时只需要按 SSE 事件流解析，不需要关心 Agent 内部状态细节

2. 后端返回类型为 `text/event-stream`

为了尽量减少缓存和缓冲，响应头中设置了：

- `Cache-Control: no-cache`
- `Connection: keep-alive`
- `X-Accel-Buffering: no`

3. `ChatService.event_stream(...)` 会按以下顺序输出事件

- `snapshot`
  - 先返回“正在思考中...”
  - 用于让前端尽快给用户可见反馈
- `delta`
  - 把最终答案拆成逐字增量事件
  - 用于构造流式打字效果
- `final`
  - 返回完整最终答案
  - 用于前端做最终落盘和兜底展示

4. 当前项目中的流式输出策略是“只展示最终答案的流式效果”

- Agent 内部可能会产生多个中间快照
- 但后端不会把完整推理过程直接暴露给前端
- 后端会保留最后一次有效答案作为 `final_answer`
- 然后再把这个最终答案逐字拆分成 `delta` 事件发给前端

5. 前端在这部分的职责边界

- 负责发起流式请求
- 负责解析 `snapshot / delta / final` 三类事件
- 负责展示用户消息、展示助手消息、展示“正在思考中”状态
- 不负责短期记忆拼接
- 不负责长期记忆处理
- 不负责工具调用和 RAG 检索逻辑

6. 主要功能：在不暴露内部推理细节的前提下，给用户提供平滑的流式回答体验
